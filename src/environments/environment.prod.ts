export const environment = {
  production: true,
  apiBaseUrl: 'https://3circle.in/backend/public', // This represents the base URL for running our frontend project. 
  prefix: '/api/v1', // Change only the domain part, keeping "/api" intact
};

